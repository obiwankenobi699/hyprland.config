# NOTE: these are re-defined (and overridden) in keybinds.conf, which is the
# effective source for app bindings. Kept here aligned to avoid confusion.
$terminal = kitty
$fileManager = thunar
$menu = wofi --show drun
$screenshot = grimblast copysave area ~/Pictures/screenshot/$(date +'%Y-%m-%d_%H-%M-%S_grim.png')
